import React from 'react'

const AddUsers = ({handleClick}) =>
<div className='addUsers-wrapper'>
<button onClick={handleClick}><span>Show Our Members </span></button>
</div>

export default AddUsers
